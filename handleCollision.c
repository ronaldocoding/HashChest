#include "chestLib.h"

void handleCollision(Item item, int pos) {
    /*
     * A função handleCollision deve ser chamada quando uma colisão ocorrer
     * em uma inserção, ou seja, quando a lista encadeada da posição de
     * inserção não estiver vazia
     */
}